# Parsing-landscape research: in-flight status (2026-09-15)

Not a course ticket — a side research push into how real ATS/job platforms parse resumes,
requested directly by the learner (not via /wayfinder). Six Haiku agents were launched to
cover: third-party parsing engines, ATS vendors, job-seeker platforms, big tech in-house.

**Bug hit and fixed**: the `research` skill's own text ("spin up a background agent to do
the research") caused a subagent that read it to spawn *another* agent instead of doing the
work — first batch of 6 all returned "launched" with nothing written. Fixed by relaunching
with direct instructions (no Skill tool, no Agent tool, do the work yourself, WebSearch/
WebFetch directly).

## Status of the six (second, fixed batch)

- [x] **ats-greenhouse-lever-workday.md** — done, file written and confirmed good.
- [x] **engines-textkernel-daxtra.md** — done. NOTE: this agent went off-script and made an
  unauthorized git commit (`cb0255f`, local only, not pushed). Checked with `git show --stat`:
  scoped cleanly to just this one file, nothing stray. Left as-is; flagged to the learner.
  Don't let future research agents touch git — should have been told explicitly not to.
- [x] **engines-hireability-rchilli.md** — done.
- [x] **ats-icims-smartrecruiters-ashby-bamboohr-taleo.md** — done.
- [x] **job-boards-linkedin-indeed-zip-glassdoor.md** — done. All six complete.
- [x] **bigtech-google-amazon-meta-microsoft-apple.md** — done, honestly thin as expected.

## Next steps once all six land

1. Check each file actually has real cited content (don't trust the agent's own "done" claim
   blindly — spot check).
2. Collate via **one more dedicated agent** (not by reading all six into this session's
   context) into a single `reference/parsing-landscape.md` index, RESOURCES.md-style: one
   section per entity, gist + links, matching the existing citation format.
3. Update RESOURCES.md's "Gaps" section to reference the new landscape doc and note what's
   still unverified/thin (expect bigtech and possibly job-boards to be sparse).
4. This whole line of research is background/context for the course, not itself a course
   ticket — it doesn't block or get logged on course/v1/map.md's Decisions-so-far.
